# frozen_string_literal: true

# Default to the Sprockets variant for local development.
# CI uses BUNDLE_GEMFILE to select gemfiles/sprockets.gemfile or gemfiles/propshaft.gemfile.
eval_gemfile "gemfiles/sprockets.gemfile"

# Linters (local development only, not used in CI)
gem "rubocop"
gem "rubocop-packaging"
gem "rubocop-performance"
gem "rubocop-rails"
gem "rubocop-rspec"
